{
  "name": "agent-game-harness",
  "version": "0.1.0",
  "private": true,
  "description": "Agent-first browser harness for ScummVM with symbolic state for AI agents",
  "scripts": {
    "bootstrap": "./scripts/bootstrap.sh",
    "build:scummvm": "./scripts/build-scummvm.sh",
    "dev": "./scripts/start-dev.sh",
    "start": "./scripts/start-dev.sh",
    "open": "./scripts/open-chrome.sh",
    "serve": "npx --yes http-server web -p 5173 -c-1 --cors"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
